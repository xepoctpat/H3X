# Standalone Environment Configuration
TEAMSFX_ENV=standalone
NODE_ENV=development

# OpenAI Configuration  

# Bot Framework Configuration
BOT_ID=local-weather-bot-$(date +%s)
BOT_PASSWORD=local-dev-password-123
PORT=3978

# Logging Configuration
DEBUG=true
LOG_LEVEL=info

# Optional: Microsoft Graph API (when you add M365 integration)
# GRAPH_CLIENT_ID=your_app_id
# GRAPH_CLIENT_SECRET=your_app_secret
# GRAPH_TENANT_ID=your_tenant_id


# GitHub Integration
GITHUB_TOKEN=your_github_token_here